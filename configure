#!/usr/bin/env bash

mkdir -p src
cd src

mgnl get robertkowalski-shareable-component
mgnl get mgnl-custom


# want to change sources:

# git clone https://github.com/topherzee/shareable-component
# git clone https://github.com/robertkowalski/mgnl-custom

# walk the directory and compile every module if needed
# cd shareable-component && cd .. # no build step
# cd mgnl-custom && npm i && npm run build
